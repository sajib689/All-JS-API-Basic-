const numbers = [2, 8, 6, 3];
const output = [];

for(number of numbers) {
    const double = number * 2;
    output.push(double);
}
console.log(output);
const makeDouble = numbers.map(b);

function b (number) {
    let d = number * 2;
}
console.log(makeDouble)